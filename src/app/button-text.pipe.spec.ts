import { ButtonTextPipe } from './button-text.pipe';

describe('ButtonTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ButtonTextPipe();
    expect(pipe).toBeTruthy();
  });
});
